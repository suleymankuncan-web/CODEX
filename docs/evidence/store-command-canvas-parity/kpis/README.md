# KPI Özetleri Command Canvas parity

Deterministic production evidence covers the accepted 1440x900, 1024x768,
390x844 and 320x844 frames. The route uses the real Store shell, live API
contracts and role-scoped hierarchy. It contains no Labs controls or fixture
data in production.

`admin-web/e2e/store-kpis-contracts.spec.ts` proves the visual frame,
company-to-region-to-store drill, local decision rails, sorting, monthly
period continuity, Profile Git navigation, partial/error truth, accessibility,
mobile input sizing and overflow safety.

Result: `Prototype parity: PASS` for KPI Özetleri.
